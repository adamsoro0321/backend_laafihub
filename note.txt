=====1.tous les endpoint doive etre tester

====Auht doive etre gere

heroku create 
git push heroku master

sup cache: heroku config:unset NODEMODULESCACHE

heroku config:set VARIABLE_NAME=value


=========heroku======
sup cache: heroku config:unset NODEMODULESCACHE

heroku config:set VARIABLE_NAME=value

heroku plugins:install heroku-repo

heroku repo:purge_cache -a nom_de_votre_application

heroku run bash

ancient-springs-10322

new Sequelize(process.env.DATABASE_URL)